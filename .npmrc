; @react-beauty/ui-checkbox@1.0.2 declares peers (@linaria ^6, react ^18) older
; than what this project uses (@linaria 7, react 19). It works at runtime, but
; strict resolution in `npm ci` (Docker/CI) fails without this flag.
legacy-peer-deps=true
